<div id="message-<%= message.id %>" class="message-box<%= message.user_id == current_user.id ? '1' : '2'%>">
  <small>

    <strong><%= message.user.first_name %></strong>
    <i><%= message.created_at.strftime("%a %b %e at %l:%M %p") %></i>
  </small>
  <p><%= message.content %></p>
</div>
